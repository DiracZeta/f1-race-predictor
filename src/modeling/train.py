"""Train the race-winner classifier on the gold layer.

The model is the LAST step of the pipeline, not the point of it. Keep it simple
and swappable — the gold table is the interface.
"""


def train():
    raise NotImplementedError
